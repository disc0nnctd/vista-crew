# Screenshots

The product without running it. All taken from the real build against the
published dataset — nothing here is a mockup.

| File | What it shows |
| --- | --- |
| `plan.png` | The flagship: ranked cover for P-2291, recommended row with the seven rule chips |
| `q31-ranked-cover.png` | The same question with the exclusion list open — nineteen candidates, each with the rule that stopped them |
| `q28-duty-timeline.png` | The duty week with the proposed cover inserted, and the rest gap that breaks it in red |
| `q01.png` | A Tier-1 lookup: reserves on call, with the on-call window column |
| `q12.png` | A lookup where the rows named in the reply are hoisted and marked |
| `q21.png` | A legal assignment whose positioning carries a cost, not a breach |
| `q23.png` | Earliest next report under the minimum-rest rule |
| `q32.png`, `q32-open.png` | Two captains lost at once: the joint plan, folded and open |
| `q33.png` | Delay recovery options after an FDP breach |
| `empty-computed.png` | "Computed, not drawn" — a result with no panel, distinguished from no result at all |

`q28-duty-timeline.png` and `q31-ranked-cover.png` are from a recorded
38-question sweep and predate the most recent interface pass; the rest are
current.
